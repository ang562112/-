# Example Dockerfile for external report-worker runtime
FROM python:3.12-slim
WORKDIR /app
COPY . /app
# RUN pip install -r requirements.txt
# CMD ["python", "-m", "report_worker.main"]
CMD ["sh", "-c", "echo 'report-worker placeholder container' && sleep infinity"]
